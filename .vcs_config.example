# VCS Configuration Example
# This file shows example configuration for upstream VCS tracking

# Upstream repository URL
# Replace with your actual upstream repository URL
upstream_url=https://github.com/FAIRmat-NFDI/nomad.git

# Remote name for upstream (default: upstream)
remote_name=upstream

# Branch to track (default: main or develop)
tracking_branch=main

# Auto-fetch from upstream (default: false)
auto_fetch=false
